<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Panther.App.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Panther.App.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{x:Bind ViewModel.WindowTitle}">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <Border Width="16" Height="16"
                        Background="#0078D4"
                        CornerRadius="3"
                        Margin="0,0,8,0">
                    <TextBlock Text="P"
                               FontSize="9"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{x:Bind ViewModel.ChromeTitle}"
                           VerticalAlignment="Center"
                           FontSize="12"/>
            </StackPanel>
        </Grid>

        <NavigationView x:Name="NavigationView"
                        Grid.Row="1"
                        PaneDisplayMode="Top"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        SelectionChanged="NavigationView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Now Playing" Tag="now-playing" Icon="Play" IsSelected="True"/>
                <NavigationViewItem x:Name="LibraryNavItem" Content="Library" Tag="library" Icon="Library"/>
                <NavigationViewItem Content="Playlists" Tag="playlists" Icon="List"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
