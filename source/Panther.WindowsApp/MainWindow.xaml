<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Panther.WindowsApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Panther.WindowsApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Panther.WindowsApp.Views.Controls"
    mc:Ignorable="d"
    Title="Panther">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28" />
            <RowDefinition />
            <RowDefinition Height="110" />
        </Grid.RowDefinitions>

        <controls:PantherTitleBar />

        <NavigationView PaneTitle="Panther" Grid.Row="1" x:Name="NavView" PaneDisplayMode="Top"
                        IsSettingsVisible="True" OpenPaneLength="240" ItemInvoked="NavView_ItemInvoked">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavItemNowPlaying" Icon="Play" Tag="NowPlaying" />
                <NavigationViewItemSeparator />
                <NavigationViewItem x:Uid="NavItemArtists" Tag="Artists">
                    <NavigationViewItem.Icon>
                        <PathIcon Data="{StaticResource ArtistsIconPath}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="NavItemAlbums" Tag="Albums">
                    <NavigationViewItem.Icon>
                        <PathIcon Data="{StaticResource DisksIconPath}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="NavItemSongs" Tag="Songs">
                    <NavigationViewItem.Icon>
                        <PathIcon Data="{StaticResource TracksIconPath}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItemSeparator />
                <NavigationViewItem x:Uid="NavItemPlaylists" Icon="List" Tag="Playlists" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource AppBarBorderThemeBrush}">
            <controls:PlayerControl Grid.ColumnSpan="2" />
        </Border>
    </Grid>
</Window>
